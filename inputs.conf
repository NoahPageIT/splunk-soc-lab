# Splunk data input - ingest the Windows Security event log.
# Install: copy into  %SPLUNK_HOME%\etc\system\local\inputs.conf  then restart Splunk.
# This is what the SOC Overview dashboard runs on.

[WinEventLog://Security]
disabled = 0
index = main
